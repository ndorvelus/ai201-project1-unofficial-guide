sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Document ingestion & chunking (Milestone 3)
requests==2.32.3
beautifulsoup4==4.12.3
tiktoken==0.14.0
playwright==1.55.0  # needed for JS-rendered pages (howard.mydininghub.com); run `playwright install chromium` once after installing

# Query interface (Milestone 5) — scripts/cli.py uses only the standard library

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
